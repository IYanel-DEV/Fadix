Act as an Elite Systems Architect and Concurrency Engineer specializing in multi-threaded runtime architectures using Tokio inside Tauri 2.0. We are implementing Module A: The Multi-Agent State Machine & Orchestrator.

[OBJECTIVE]
Write the complete, production-ready Rust backend implementation for the Multi-Agent Orchestrator inside 'src-tauri/src/agents/'. This engine manages state persistence across a central ledger and steps sequentially through task lists without blocking the main OS UI thread.

[TECHNICAL REQUISITES & SCENARIOS]
1. HARDWARE STATE DATA MODEL ('src-tauri/src/state/'):
   - Implement the serialization schemas for 'workspace_state.json' and 'current_task.json' using structs.
   - 'WorkspaceState' must track: 'active_provider', 'target_directory', 'current_phase' (Idle, Planning, Coding, Verifying, Complete, Failed), and 'agent_statuses' (HashMap of agent keys to an Enum of Idle, Working, Error).
   - 'CurrentTask' must represent the Architect's output structure: 'user_original_query', 'execution_plan' (an array of steps containing 'assigned_agent', 'action_description', and 'file_targets'), and a validation field.

2. THE CENTRAL CONDUCTOR ('orchestrator.rs'):
   - Create a central 'Orchestrator' manager struct utilizing an asynchronous execution loop powered by 'tokio::sync::mpsc'.
   - It must implement a 'run_task_pipeline(user_query: String, app_handle: tauri::AppHandle)' execution path.
   - PHASE 1 (Planning): Wake up the 'Architect' agent via our LLM router. Inject strict prompt contexts forcing it to output valid JSON matching our 'CurrentTask' structure. Write this payload directly to disk as 'current_task.json' within the workspace.
   - PHASE 2 (Sequenced Execution): Read the array of execution steps sequentially. For each step, look up the assigned specialist (Coder, Backend, or UI), pass them the current task ledger along with the specific files matching the target list, and update the global state ledger.
   - PHASE 3 (Validation Hook): After modifications are completed, evaluate step success parameters. If verification markers fail, trigger an automatic remediation branch or drop into a failed loop gracefully.

3. REACTION LOG EMITTER ('commands/orchestrator.rs'):
   - Register the primary Tauri command 'execute_agent_workflow'.
   - Ensure the orchestration loop broadcasts state mutations directly to the React frontend using 'app_handle.emit("agent-phase-change", ...)' every time a phase changes, a token processes, or a specialist changes its internal status state.

[OUTPUT REQUIREMENT]
Provide the complete, production-grade Rust implementation code for these modular execution components. Do not leave placeholder shortcuts or write loose boilerplate. Ensure robust error boundaries where all exceptions broadcast a structured 'Failed' phase update to the client before exiting cleanly.